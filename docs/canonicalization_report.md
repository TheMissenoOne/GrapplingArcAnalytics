# Canonicalization review — technique_nodes

Threshold (cosine ≥): **0.92** · same-type only.
Nodes: **437** → projected canonical **384** (36 clusters merge 53 aliases away).

> Review artifact. No DB writes. Confirm/override each cluster before any merge.

## Attempt nodes (62) — outcome → edge, not a node
americana attempt, arm triangle attempt, armbar attempt, armbar choi bar attempt, armbar triangle attempt, armtriangle attempt, armtriangle headarm strangle attempt, back take attempt, body lock takedown attempt, body triangle attempt, buggy choke attempt, calf slicer attempt, choi bar attempt, darce choke attempt, double leg takedown attempt, drop seoi nage attempt, ezekiel attempt, ezekiel choke attempt, face crank attempt, flying darce attempt, flying triangle attempt, foot lock attempt, foot sweep attempt, guard pass attempt, guillotine attempt, guillotine choke attempt, head and arm choke attempt, heel hook attempt, inside heel hook attempt, inversion attempt, inverted triangle attempt, kata gatame attempt, katagatame darce attempt, katagatame gift wrap attempt, kimura attempt, knee bar attempt, leg drag pass attempt, leg entanglement attempt, leg entanglement inside heel hook attempt, leg entry attempt, leg lock attempt, mount attempt, north south choke attempt, northsouth choke attempt, omoplata attempt, omoplata triangle attempt, pull guard attempt, rear naked choke attempt, rear naked choke attempt onearm, single leg takedown attempt, standing guillotine attempt, straight ankle lock attempt, straight foot lock attempt, submission attempt, sweep attempt, takedown attempt, toe hold attempt, triangle attempt, triangle choke attempt, twister attempt, uchi mata attempt, wrist lock attempt

## Out-of-scope node_types
- **event class** (26): blood time, blooper, body kick, body punch, caution penalty, challenge review, combination, counter, cross, elbow, front kick, head kick, injury timeout, jab, knee, knockdown, left hook, leg kick, match, overhand, right hook, spinning attack, stalling warning, strike, taunt, uppercut
- **remap** (7): arm drag, berimbolo, lapel guard techniques, leg drag pass, singleleg defense, sprawl, worm guard

## Cross-type near-duplicates (0) — likely mislabels

## Duplicate clusters (36)

### [takedown] Double-leg takedown `(library)`
- Single-leg takedown `singleleg takedown` (library) ⚠️ review — semantic only
- Single Leg `single leg` (user) ⚠️ review — semantic only
- Double Leg Takedown Attempt `double leg takedown attempt` (user)
- Single Leg Takedown `single leg takedown` (user) ⚠️ review — semantic only
- Single Leg Takedown Attempt `single leg takedown attempt` (user) ⚠️ review — semantic only

### [submission] Heel Hook `(library)`
- Inside Heel Hook Attempt `inside heel hook attempt` (user)
- Counter Heel Hook `counter heel hook` (library)
- Heel Hook Attempt `heel hook attempt` (user)
- Reverse Heel Hook `reverse heel hook` (library)

### [submission] Armbar Attempt `(user)`
- Arm Triangle Attempt `arm triangle attempt` (user) ⚠️ review — semantic only
- Arm‑Triangle Attempt `armtriangle attempt` (user) ⚠️ review — semantic only
- Armbar / Triangle Attempt `armbar triangle attempt` (user)

### [submission] Ankle Lock `(library)`
- Straight Ankle Lock `straight ankle lock` (user)
- Ankle Lock Grip `ankle lock grip` (user)
- Straight Ankle Lock Attempt `straight ankle lock attempt` (user)

### [submission] North South Choke `(library)`
- North-South Choke `northsouth choke` (user)
- North South Choke Attempt `north south choke attempt` (user)
- North‑South Choke Attempt `northsouth choke attempt` (user)

### [submission] Rear Naked Choke `(library)`
- Rear Naked Choke Attempt (One-Arm) `rear naked choke attempt onearm` (user)
- Rear Naked Choke Attempt `rear naked choke attempt` (user)

### [submission] Reverse Triangle `(library)`
- Inverted Triangle `inverted triangle` (user) ⚠️ review — semantic only
- Inverted Triangle Attempt `inverted triangle attempt` (user) ⚠️ review — semantic only

### [guard] Leg Entanglement `(user)`
- Leg Lock Entanglement `leg lock entanglement` (user)
- Leg Entanglement / 50/50 `leg entanglement 5050` (user)

### [submission] Wristlock `(library)`
- Wrist Lock `wrist lock` (library) ⚠️ review — semantic only
- Wrist Lock Attempt `wrist lock attempt` (user) ⚠️ review — semantic only

### [submission] Twister `(library)`
- Twister Attempt `twister attempt` (user)

### [transition] Pull Guard `(library)`
- Guard Pull `guard pull` (user)

### [takedown] Trip `(user)`
- Trip Takedown `trip takedown` (user)

### [submission] Leg Lock `(library)`
- Leg Lock Attempt `leg lock attempt` (user)

### [submission] Straight Foot Lock `(user)`
- Straight Foot Lock Attempt `straight foot lock attempt` (user)

### [escape] Upa (Bridge) Escape `(library)`
- Bridge (Upa Escape) `bridge upa escape` (user)

### [transition] Leg Entry `(library)`
- Leg Entry Attempt `leg entry attempt` (user)

### [submission] Toe Hold `(library)`
- Toe Hold Attempt `toe hold attempt` (user)

### [submission] Kneebar `(library)`
- Knee Bar `knee bar` (user) ⚠️ review — semantic only

### [submission] Americana `(library)`
- Americana Attempt `americana attempt` (user)

### [submission] Face Crank `(user)`
- Face Crank Attempt `face crank attempt` (user)

### [takedown] Ankle Pick `(library)`
- Ankle Pick Takedown `ankle pick takedown` (user)

### [control] Snap Down / Front Headlock `(user)`
- Snap Down to Front Headlock `snap down to front headlock` (user)

### [takedown] Foot Sweep `(user)`
- Foot Sweep Attempt `foot sweep attempt` (user)

### [strike] Left Hook `(user)`
- Right Hook `right hook` (user) ⚠️ review — semantic only

### [escape] Turtle Escape `(user)`
- Escape to Turtle `escape to turtle` (user)

### [submission] Foot Lock `(user)`
- Foot Lock Attempt `foot lock attempt` (user)

### [submission] Reverse Arm Lock `(library)`
- Arm Lock `arm lock` (user)

### [submission] Flying Triangle `(user)`
- Flying Triangle Attempt `flying triangle attempt` (user)

### [submission] Darce Choke `(library)`
- Darce Choke Attempt `darce choke attempt` (user)

### [submission] Submit `(user)`
- Submission `submission` (user) ⚠️ review — semantic only

### [submission] Buggy Choke `(user)`
- Buggy Choke Attempt `buggy choke attempt` (user)

### [submission] Guillotine Choke `(library)`
- Guillotine Choke Attempt `guillotine choke attempt` (user)

### [submission] Calf Slicer `(library)`
- Calf Slicer Attempt `calf slicer attempt` (user)

### [pass] Knee Cut `(user)`
- Knee Cut Pass `knee cut pass` (user)

### [submission] Triangle Choke `(library)`
- Triangle Choke Attempt `triangle choke attempt` (user)

### [submission] Ezekiel Choke `(library)`
- Ezekiel Choke Attempt `ezekiel choke attempt` (user)
